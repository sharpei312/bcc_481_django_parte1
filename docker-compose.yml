# Docker Compose para ejecutar el servicio web de Django
services:
  web:
    build: .
    container_name: demo-django
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    command: >
      sh -c "python manage.py migrate &&
             python manage.py runserver 0.0.0.0:8000"